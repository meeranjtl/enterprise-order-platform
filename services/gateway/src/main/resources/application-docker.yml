# Docker profile: container hostnames instead of localhost.
# Spring Boot REPLACES list properties on profile merge (it does not merge them),
# so the FULL routes list must be re-declared here — not just the changed URIs.
spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      # Centralized CORS for browser clients (React UI arrives in Phase 13)
      globalcors:
        cors-configurations:
          "[/**]":
            allowedOriginPatterns:
              - "http://localhost:*"
            allowedMethods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
            allowedHeaders: [Authorization, Content-Type, X-Correlation-Id, X-Requested-With, Accept]
            exposedHeaders: [X-Correlation-Id]
            allowCredentials: true
            maxAge: 3600

      # Applied to every route: Redis-backed token bucket, keyed by client IP.
      # Per-endpoint tuning: add a RequestRateLimiter filter with its own
      # redis-rate-limiter.* args to an individual route (overrides this default).
      default-filters:
        - name: RequestRateLimiter
          args:
            redis-rate-limiter.replenishRate: 10
            redis-rate-limiter.burstCapacity: 20
            redis-rate-limiter.requestedTokens: 1
            key-resolver: "#{@ipKeyResolver}"

      routes:
        - id: customer-service
          uri: http://customer-service:8081
          predicates:
            - Path=/api/v1/customers/**
          filters:
            - name: CircuitBreaker
              args:
                name: customerService
                fallbackUri: forward:/fallback/customer-service

        - id: product-service
          uri: http://product-service:8082
          predicates:
            - Path=/api/v1/products/**
          filters:
            - name: CircuitBreaker
              args:
                name: productService
                fallbackUri: forward:/fallback/product-service

        - id: order-service
          uri: http://order-service:8083
          predicates:
            - Path=/api/v1/orders/**
          filters:
            - name: CircuitBreaker
              args:
                name: orderService
                fallbackUri: forward:/fallback/order-service

        - id: inventory-service
          uri: http://inventory-service:8084
          predicates:
            - Path=/api/v1/inventory/**
          filters:
            - name: CircuitBreaker
              args:
                name: inventoryService
                fallbackUri: forward:/fallback/inventory-service

        - id: payment-service
          uri: http://payment-service:8085
          predicates:
            - Path=/api/v1/payments/**
          filters:
            - name: CircuitBreaker
              args:
                name: paymentService
                fallbackUri: forward:/fallback/payment-service

        - id: customer-service-api-docs
          uri: http://customer-service:8081
          predicates:
            - Path=/customer-service/api-docs
          filters:
            - RewritePath=/customer-service/api-docs, /api-docs

        - id: product-service-api-docs
          uri: http://product-service:8082
          predicates:
            - Path=/product-service/api-docs
          filters:
            - RewritePath=/product-service/api-docs, /api-docs

        - id: order-service-api-docs
          uri: http://order-service:8083
          predicates:
            - Path=/order-service/api-docs
          filters:
            - RewritePath=/order-service/api-docs, /api-docs

        - id: inventory-service-api-docs
          uri: http://inventory-service:8084
          predicates:
            - Path=/inventory-service/api-docs
          filters:
            - RewritePath=/inventory-service/api-docs, /api-docs

        - id: payment-service-api-docs
          uri: http://payment-service:8085
          predicates:
            - Path=/payment-service/api-docs
          filters:
            - RewritePath=/payment-service/api-docs, /api-docs

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info,gateway
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true   # expose /actuator/gateway/routes|routefilters|globalfilters

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    urls:
      - name: Customer Service
        url: /customer-service/api-docs
      - name: Product Service
        url: /product-service/api-docs
      - name: Order Service
        url: /order-service/api-docs
      - name: Inventory Service
        url: /inventory-service/api-docs
      - name: Payment Service
        url: /payment-service/api-docs

logging:
  level:
    com.enterprise.order: INFO
    org.springframework.cloud.gateway: INFO
    io.github.resilience4j: INFO

resilience4j:
  circuitbreaker:
    configs:
      default:
        slidingWindowType: COUNT_BASED
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
        permittedNumberOfCallsInHalfOpenState: 3
        registerHealthIndicator: true
    instances:
      customerService:
        baseConfig: default
      productService:
        baseConfig: default
      orderService:
        baseConfig: default
      inventoryService:
        baseConfig: default
      paymentService:
        baseConfig: default
  # The Spring Cloud Gateway CircuitBreaker filter applies a TimeLimiter whose
  # default timeout is 1s — override explicitly or every slow call would time out.
  timelimiter:
    configs:
      default:
        timeoutDuration: 10s
        cancelRunningFuture: true
    instances:
      customerService:
        baseConfig: default
      productService:
        baseConfig: default
      orderService:
        baseConfig: default
      inventoryService:
        baseConfig: default
      paymentService:
        baseConfig: default