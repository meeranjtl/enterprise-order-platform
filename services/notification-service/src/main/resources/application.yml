spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:postgresql://localhost:5432/enterprise_order?currentSchema=notification
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    schemas: notification
    default-schema: notification
    table: flyway_schema_history_notification
    create-schemas: true
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8087

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info
