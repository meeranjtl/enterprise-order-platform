# Gateway base URL — all requests route through here (see docs/architecture.md).
# Falls back to http://localhost:8080 in code if unset, so this file is optional for local dev.
VITE_API_URL=http://localhost:8080
